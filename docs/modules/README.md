# docs/modules — module documentation index

Per-module guides for the platform: alerts, api, domains, events, export,
geo-intel, gui, llm, logger, monitoring, pages, scraping, shared,
v2-intelligence, verification (one .md each; overview in AGENTS.md).
