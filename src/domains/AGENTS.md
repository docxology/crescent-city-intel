# domains/ — agent notes

Two modules (verified): coverage.ts (CLI-runnable, writes output/domain-coverage.json,
imports logger + ../domains.js registry) and scholarly_context.ts. Domain registry
itself lives in `../domains.ts`; keep this folder for per-domain computations only.
