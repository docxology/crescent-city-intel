# src/domains

Domain-level intelligence helpers:
- `coverage.ts` — domain coverage metrics: % of municipal code sections cross-referenced by each domain. Run: `bun run src/domains/coverage.ts`; writes `output/domain-coverage.json`.
- `scholarly_context.ts` — scholarly-context support (see parent ../AGENTS.md).
