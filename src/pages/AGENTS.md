# pages/ — agent notes

Pages snapshot surface (verified: static/ with 9 HTML files, including directory.html (verified-establishments directory, artifact data/directory.json from seed pages-data/directory.json), + assets/{404.css,
index.css, site.css, site.js}). Distinct from src/gui (live Bun server).
`.claude/` scratch under static/ is out of scope.
