# fixtures/ — agent notes

Offline captured responses (verified: event-discovery/ with 4 files listed in
../README.md) so event-discovery tests run without network. Keep captures
deterministic; never fetch live data in tests — add new captures instead.
