# event-discovery fixtures

Captured HTML/JSON responses from local event sources (city calendar listing,
get_list JSON, DNACA arts calendar, library programs) used as offline inputs
for event-discovery tests.
